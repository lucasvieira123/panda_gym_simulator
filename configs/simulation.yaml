simulation:
  seed: 42
  episodes: 3
  max_steps: 50
  render_mode: "human"
  step_delay: 3.00
  verbose: true

configs:
  environment_config: "environment_with_two_obstacles.yaml" # "environment.yaml" "environment_with_obstacle.yaml"
  target_goal_config: "target_goal.yaml"
  scripts_file: "scripts.yaml"
  #script_id: "left_right"
